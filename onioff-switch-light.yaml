# These substitutions allow the end user to override certain values
substitutions:
  project_version: "1.0.6"
  device_name: "Light"
  device_type: "light"
  device_platform: "binary"
  device_icon: "mdi:lightbulb"
  restore_mode: "ALWAYS_ON"

esphome:
  name: "onioff-switch-light"
  friendly_name: "ONiOFF Light Switch"
  project:
    name: curtistinkers.onioff-switch-light
    version: ${project_version}

# This should point to the public location of this yaml file.
dashboard_import:
  package_import_url: github://curtistinkers/esphome-onioff-smart-switch/onioff-switch-light.yaml@main

packages:
  onioff-base: !include
    file: common/onioff-base.yaml
    vars:
      device_name: ${device_name}
      device_type: ${device_type}
      device_platform: ${device_platform}
      device_icon: ${device_icon}
      restore_mode: ${restore_mode}

